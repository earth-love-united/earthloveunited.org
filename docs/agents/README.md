# Agent Notes

This directory is reserved for agent-specific references and handoff notes.

The current quick reference remains at `../../.agent-context.md` because
`../../AGENTS.md` still documents that path. Moving it should happen with the
same protected-file update that moves or redirects the root governance docs.
